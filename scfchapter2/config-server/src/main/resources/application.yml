eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8889/eureka/

spring:
  application:
    name: config-server
  cloud:
      config:
        server:
          git:
            uri: https://github.com/GoodGirl312/springcloud/
            search-paths: respo2
        label: master
server:
  port=8888



